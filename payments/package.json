{
  "name": "playman-payments",
  "private": true,
  "type": "module",
  "description": "PaySwitch (theTeller) mobile money collection for the Play Man Lounge order form",
  "scripts": {
    "dev": "node --experimental-strip-types src/dev-server.ts",
    "test": "node --experimental-strip-types --test src/*.test.ts",
    "sync:prices": "node --experimental-strip-types scripts/sync-prices.ts",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
